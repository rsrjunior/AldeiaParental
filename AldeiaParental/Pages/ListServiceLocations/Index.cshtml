@page
@model AldeiaParental.Pages_ListServiceLocations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page-handler="Csv">Download CSV</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Cuidador
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ServiceLocation[0].Region)
            </th>
            <th>
                Descrição
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ServiceLocation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region.Name)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Description)
                @(item.AtCustomerHome?"(à domicílio)":null)
            </td>
            <td>
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
